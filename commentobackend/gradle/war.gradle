apply plugin: "war"

bootWar {
    mainClassName = "com.test.commento.CodetestbackendApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
